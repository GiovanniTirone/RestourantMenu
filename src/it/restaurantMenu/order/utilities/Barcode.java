package it.restaurantMenu.order.utilities;

public class Barcode {

    /*public static Cell createBarcode(String code, PdfDocument pdfDoc) {

        BarcodeEAN barcode = new BarcodeEAN(pdfDoc);
        barcode.setCodeType(BarcodeEAN.EAN8);
        barcode.setCode(code);

        PdfFormXObject barcodeXObject = barcode.createFormXObject(null, null, pdfDoc);
        //Cell cell = new Cell().add(new Image(barcodeXObject).scale(1.5f, 1.5f));
        Cell cell = new Cell().add(new Image(barcodeXObject).scale(0.8f, 0.8f));
        //Cell cell = new Cell().add(new Image(barcodeXObject));
        cell.setPaddingTop(10);
        cell.setPaddingRight(10);
        cell.setPaddingBottom(10);
        cell.setPaddingLeft(17.5f);

        return cell;
    }*/

}
